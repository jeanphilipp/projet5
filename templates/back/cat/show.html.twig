{% extends 'base.html.twig' %}
{% block title %}Les réservations{% endblock %}
{#% block body %#}

    {# dump()}
    {% block content %}
        {% for booking in cat.bookings %}
        {% for cat in cats %}
        {{ dump(booking) }
            {{ cat.id }}
        {% endfor %}
    {% endblock %}
{% endblock %#}
   {% block content %} {{ cat.catName }}  {% endblock %}